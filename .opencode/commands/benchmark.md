---
description: Benchmark live page performance and compare against baselines
agent: benchmark
subtask: true
---
Run the performance benchmarking workflow for the target app or page set.

Requirements:

- capture or load a baseline when available
- measure performance with real browser metrics
- compare current timing and resource data against the baseline
- report regressions with concrete thresholds and evidence
- end with a structured benchmark report and performance verdict
