---
description: Verify or connect a visible browser workflow when supported
agent: connect-chrome
subtask: true
---
Run the closest OpenCode equivalent of gstack's connect-chrome workflow.

Requirements:

- check browser adapter status first
- verify whether headed or visible browser control is actually available
- explain capability gaps plainly if OpenCode does not expose gstack's Side Panel behavior
- provide next steps for direct browsing, QA, or visual verification after connection succeeds
